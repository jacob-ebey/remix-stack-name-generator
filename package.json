{
  "private": true,
  "name": "genre-api",
  "type": "module",
  "scripts": {
    "start": "wrangler dev --local",
    "publish": "wrangler publish",
    "scrape-wiki": "deno run --allow-net --allow-read --allow-write ./scripts/scrape-wiki.ts"
  },
  "dependencies": {
    "preact": "^10.7.2",
    "preact-render-to-string": "^5.2.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^3.10.0",
    "typescript": "^4.6.4",
    "wrangler": "2.0.3"
  }
}
